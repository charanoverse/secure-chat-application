SECURE-CHAT-APP/
│
├── .venv/                         # Python virtual environment
├── dist/                          # Build/distribution artifacts (likely from PyInstaller)
│
├── secure_chat/                   # Main application package
│   │
│   ├── crypto/                    # Cryptography layer
│   │   ├── aead.py                # AEAD symmetric encryption (AES-GCM, ChaCha20-Poly1305, etc.)
│   │   ├── handshake_rsa.py       # RSA-based handshake/identity authentication
│   │   └── handshake_x25519.py    # X25519 key exchange handshake (Diffie-Hellman)
│   │
│   ├── net/                       # Networking utilities
│   │   └── framing.py             # Message framing / encoding for network packets
│   │
│   ├── relay/                     # Relay server (acts as message forwarder)
│   │   └── relay_server.py        # Implementation of relay server logic
│   │
│   ├── services/                  # High-level services used by the UI and networking
│   │   ├── chat_service.py        # Core chat logic (sending/receiving encrypted messages)
│   │   └── ephemeral_privacy.py   # Ephemeral key mgmt / forward secrecy mechanisms
│   │
│   ├── storage/                   # Persistent storage / local data handling
│   │   ├── app_config.py          # Application configuration handling
│   │   ├── generate_keys.py       # Generates client RSA/X25519 keypairs
│   │   ├── history.py             # Chat history storage logic
│   │   └── logs.py                # Logging utilities
│   │
│   ├── ui/                        # GUI (likely PyQt/PySide)
│   │   ├── __init__.py
│   │   ├── chat_widget.py         # Widget for chat messages
│   │   ├── client_window.py       # Main client UI window
│   │   ├── client_worker.py       # Background thread handling network events
│   │   ├── connect_dialog.py      # Dialog for connecting to server/relay
│   │   ├── main_window.py         # Entry-point window of the application
│   │   ├── search_dialog.py       # Search functionality in chat history
│   │   ├── server_window.py       # UI for server components (if included)
│   │   ├── server_worker.py       # Worker thread for server tasks
│   │   ├── styles.css             # UI styling
│   │   └── ttl_dialog.py          # Dialog for setting TTL (message expiration)
│   │
│   ├── __init__.py
│   ├── cli.py                     # CLI interface for the app
│   └── gui.py                     # GUI launch entry-point
│
├── secure_chat.egg-info/          # Package metadata (generated by setuptools)
│
├── chat_history.enc               # Encrypted chat history file
├── chat_history.db                # SQLite database (maybe unencrypted or legacy)
│
├── pyproject.toml                 # Build configuration / dependencies
├── README.md                      # Project documentation
│
├── requirements.txt               # Python package dependencies
│
├── server_priv.pem                # Server private key (⚠️ sensitive)
├── server_pub.pem                 # Server public key
│
├── structure.txt                  # Probably your existing structure documentation
└── usage.txt                      # Instructions for running the app
